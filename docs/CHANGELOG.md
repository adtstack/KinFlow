# Documentation Changelog

## Unreleased — 2026-07-30 — Phase 00 decisions and Phase 01/02 implementation

- Android 단일 출시, dev/prod 두 환경, 개인 운영 주체, Google 로그인 승인(D-002, D-032, D-052~D-054, ADR-0002)
- production `me.newlines.kinflow`, dev `me.newlines.kinflow.dev` 식별자 승인
- iOS/iPadOS, APNs와 staging은 Android Beta review 이후로 연기
- G0 전체 통과 전 로컬·가역적인 Phase 01 WP01-01 foundation 작업만 조건부 허용
- 대한민국 단일 공개 출시와 Supabase Seoul `ap-northeast-2` 승인(D-039)
- Store MVP를 성인 계정 사용자로 한정하고 Managed Child/child mode를 P1로 연기(D-013)
- 성인 2인 Activation vertical slice 승인(D-051, ADR-0001)
- PRD, roadmap, requirements, Phase 00/02/07, traceability와 release checklist를 승인 범위에 맞게 정렬
- 사용자 연구, 법률·Store 검토, 가격·보관 정책, 앱 식별자·console owner, 실제 기기/provider PoC는 미완료 상태 유지
- WP02-01 인증 session/PKCE 저장에 Android Keystore-backed `flutter_secure_storage 10.3.1`을 고정하고 Supabase runtime composition과 backup-disabled 계약 추가
- WP02-02 가구 authorization boundary에 IANA timezone 검증, 정확히 한 active Owner invariant, role helper, removed-member stale selection 차단과 62개 pgTAP 공격 테스트 추가
- WP02-03 첫 가구·Owner membership·profile·active selection 원자적 생성과 onboarding/empty Today 구현
- WP02-04 hash-only 단일 사용 초대, create/preview/accept/revoke Edge command와 로그인 전후 App Link continuation 구현
- dev Google/Supabase provider, `adtstack.github.io` Digital Asset Links와 Android verified App Link 준비 완료; 실제 성인 2계정·2기기 live E2E는 `0/27 NOT RUN` 유지
- WP02-05 역할 변경·제거·나가기·최근 인증 Owner 이전·immutable audit 구현과 DB 271/271, invite Edge 22/22, member Edge 18/18 검증
- WP06-03A/B provider-neutral billing flow와 exact `purchases_flutter 10.8.0` Android adapter/app composition 구현; server-authoritative confirmation, custom authenticated identity, keyless fallback, Play Billing permission와 native build 검증 추가(실계정·Store sandbox·기기는 마지막 Billing Gate로 유지)
- WP06-04 RevenueCat full Authorization + raw-body HMAC ingress, concurrent-safe metadata-only replay inbox, leased subscriber API reconciliation, periodic repair, strict lifecycle mapping, bounded retry/dead-letter와 aggregate health 구현(RevenueCat/Play 실계정·secret·hosted scheduler·기기는 마지막 Billing Gate로 유지)
- WP06-05 explicit Owner/Admin household preflight, 30분 provisional→verified confirmed binding, concurrent one-active invariants, privacy-minimized conflict/status, `ASSIGNMENT_REQUIRED` requeue, membership-drift preservation과 versioned service-only audited remediation/atomic transfer를 DB·Flutter에 구현(Provider ownership·support UI·실계정/Store sandbox/기기는 마지막 Billing Gate로 유지)
- WP06-06 D-027 수치를 임의로 정하지 않고 양 요금제 finalized 후 versioned activation하는 feature enforcement, aggregate household gate, active member와 recurring chore/calendar server trigger, advisory-lock 동시성, downgrade data preservation 및 Flutter lifecycle/한도 오류 UX를 구현(실제 수치 정책·paywall·실계정/Store sandbox/기기는 마지막 Billing Gate로 유지)
- WP06-02A active-household subscription settings/paywall에 Store-localized 가격·기간, Owner/Admin purchase/restore 확인, pending 중복 차단, assignment/restore conflict review, 서버 lifecycle·갱신·billing-owner 상태, Google Play/Apple 관리 및 terms/privacy/support allowlist 외부 링크, EN/KO/EN-XA 200% UI를 구현(D-027 실제 수치·실계정/Store sandbox/실기기는 마지막 Billing Gate로 유지)
- WP07-01 앱 내 계정 삭제 preflight/request/status/cancel, 동일 사용자 최근 OAuth, 기본 24시간 취소, last-Owner·active subscription gate, leased tombstone/Auth soft-delete worker와 설정 UI·logout/cache/provider cleanup handoff를 구현(shared household data 보존; 공개 웹·법률 승인·hosted scheduler·실계정/Store/실기기는 후속 Gate)
- WP07-02A 본인 범위 개인 데이터 export를 JSON/TXT로 생성하고 private Storage, hash-only 5분 일회성 다운로드, 기본 24시간 expiry, revoke/purge/retry와 설정 UI에 구현(Owner full-household export, hosted lifecycle/scheduler, 실계정/browser/다중기기/실기기는 후속 Gate)
- WP07-02B current Owner 전용 shared-household JSON/TXT export와 파일당 20 MiB private artifact, hash-only 5분 one-time download, exact-name/영향 확인 24시간 cooling-off 가구 삭제, retention hold·RLS 접근 차단·redaction·billing unlink worker 및 EN/KO/EN-XA 설정 UI를 구현(hosted Storage/scheduler, 실계정/browser/Store/다중기기/실기기는 마지막 Gate)
- WP07-03A primary 초대 링크의 8-symbol 보조 코드를 hash-only·최대 24시간으로 발급하고 public/user별 10회/10분 lockout, generic terminal error, 기존 transactional accept, process-memory 로그인 continuation과 EN/KO/EN-XA 수동 입력 UI를 구현(hosted WAF/proxy·실계정·Android clipboard/keyboard 검증은 마지막 Gate)
- WP07-07B 기존 Android association을 보존한 Astro EN/KO no-JS 공개 terms/privacy/support/account-deletion 사이트와 앱 재설치 없는 content-free email 요청 경로, CSP/accessibility/static contract, draft·법률 본문·owned origin/mailbox production fail-closed를 구현(final legal/retention 승인·owned 배포·mailbox/support handoff·실browser/screen-reader/실기기는 마지막 Gate)
- WP07-06A 성인 본인 display name·번들 avatar preset·EN/KO locale·개인 IANA timezone과 Owner/Admin 가구 기본 timezone을 atomic expected-version RPC로 저장하고 active membership 동기화·private audit·기존 반복 timezone 보존 안내·즉시 locale 반영·로그아웃 격리·EN/KO/EN-XA 200% UI를 구현(hosted catalog·실계정 cross-device/process restart·screen reader·실기기 검증은 마지막 Gate)
- WP07-06B 기존 `timezone 0.11.1` IANA database `2025c`를 공유 초기화해 network-free 지역·도시 검색, current offset/DST, selected/UTC 우선·최대 100건 결정적 결과와 개인/Owner/Admin 재사용 선택기를 추가하고 자유 입력을 제거했으며 failure draft 보존·retry와 EN/KO/EN-XA compact 200% 전체-scroll UI를 검증(hosted PostgreSQL parity·실계정/cross-device/process restart·screen reader·실기기는 마지막 Gate)
- WP07-06C 공용 시간대 선택기를 첫 가구 생성과 알림 preference까지 확장해 Store MVP 편집 가능한 timezone 자유 입력을 0개로 만들고, London 선택 후 exact 가구 create·New York 선택 후 quiet-hours/version 보존 save·cancel 무변경·EN/KO/EN-XA 회귀를 검증(hosted PostgreSQL parity·실계정 onboarding/알림 저장·cross-device·실기기는 마지막 Gate)
- WP07-06D profile regional card에 같은 UTC instant 기준 개인·가구 현재 날짜/시간 비교를 추가하고, 저장 전 EN/KO·IANA timezone draft와 system 12/24-hour 형식을 즉시 반영하며 명시적 refresh·failure snapshot 보존·missing identifier fail-closed·EN-XA 200%/48dp를 검증(DB/API 변경 없음; hosted parity·실계정 cross-device/process restart·DST 전환·screen reader·실기기는 마지막 Gate)
- WP01-08 Android 알림·Store 결제·암호화 로컬 저장소·외부 링크·background delivery의 exact provider/fallback registry를 production/unavailable composition에 연결하고, 알림 권한 미결정·거부·runtime unavailable·일시 실패와 의도된 server-authoritative 제한을 설정의 기기 기능 상태 화면에서 EN/KO/EN-XA 200%·48dp로 설명하며 기존 알림·구독·진단 route로 안내(DB/API/provider 호출 없음; 실제 Firebase·RevenueCat/Play·Keystore·system handler·실기기와 Web/iOS는 후속 Gate)
- WP01-09 기기 기능 상태에 exact 준비됨·조치 필요·대안/제한 집계, temporary→action→fallback→limited 복구 순서와 기존 안전 route를 추가하고, 시스템 prompt/settings를 열지 않는 명시적 single-flight 알림 권한·기기 연결 재확인을 기존 coordinator에 연결했다(DB/API/새 provider 없음; 실제 settings 왕복·Firebase·실계정·다중기기·실기기는 마지막 Gate)
- WP01-10 exact six typed usage event와 five-field content-free envelope, 기본 OFF `analytics-usage-v1` secure device preference, Managed Child 선차단·nonblocking unavailable sink·authenticated-entry dedupe를 구현하고 Settings에 optional collection 상태와 Sentry/Firebase/RevenueCat/Google+Supabase SDK 목적을 EN/KO/EN-XA로 공개했다(외부 behavioral SDK·DB/API·새 permission 없음; approved provider·법률 동의·server consent·실계정·실기기는 마지막 Gate)
- WP01-11 authenticated core surface에 Today·Chores·Calendar·Family·Settings exact five route를 compact NavigationBar·medium collapsed rail·expanded extended rail로 연결하고, upcoming/overdue/completed와 Everyone/Me만 제공하는 `/chores` 허브 및 `/family` primary route·기존 members alias를 구현했다(DB/API/state persistence/telemetry 없음; 실제 계정·TalkBack·phone/tablet 실기기는 마지막 Gate)
- WP01-12 같은 Flutter codebase에 독립 dev/prod Web release build와 path URL, email OTP 우선 sign-in, package `kinflow_app`·platform `web` runtime policy, exact-five safe fallback을 추가하고 PWA manifest·service worker·영속 API cache·Web Push·Web 구매를 비활성화했다. local browser에서 sign-in과 policy read를 확인했으며 hosted HTTPS/CSP/SPA rewrite·OAuth callback·실계정/browser matrix는 독립 Web Gate로 유지한다(D-070).
- WP10-01A Web 성인 초대의 명시적 Share action을 조건부 `navigator.share` adapter에 연결하고 canonical HTTPS URL과 localized title만 전달했으며, unsupported/rejection은 raw browser detail 없는 상태로 축약하고 자동 copy 없이 기존 별도 write-only clipboard fallback과 Android chooser를 보존했다(DB/API/storage/permission/새 dependency 없음; hosted Permissions-Policy·실browser·recipient·실계정·App Link는 마지막 Web Gate).
- WP10-03A Web에서 keyboard focus highlight를 traditional mode로 고정하고 exact-five core route를 합성 인증 상태에서 Tab/Enter만으로 순회했으며, 앱 소유 dialog 26개·modal sheet 4개를 request-focus·closed-loop·opener focus return 공용 계약으로 통합했다(DB/API/dependency/telemetry 변경 없음; 실제 browser 200% zoom·screen reader·hosted 인증·실계정·물리 키보드는 마지막 Web Gate).
- WP10-03B Web 보호 route에 fixed continuation marker와 same-runtime session-expiry exact 복귀·refresh 후 coarse 복귀를 추가하고, explicit logout·identity/household 전환에서 이전 상세 intent를 폐기했으며 unknown/invalid 동적 URL을 공용 404로 통합했다(DB/API/dependency/storage/telemetry 변경 없음; hosted SPA rewrite·실browser history/BFCache·실계정 전환은 마지막 Web Gate).
- WP02-10 Google 로그인에서 Supabase exact identity conflict code 세 개만 stable `IDENTITY_CONFLICT`로 분류하고, 자동 병합·link 없이 conflict 전용 Google account selection 초기화, 명시적 다른 계정 single-flight 재시도와 enum-only support 경로를 EN/KO/EN-XA 200% UI로 구현했다(DB/API/storage/새 SDK 없음; hosted identity policy·실제 충돌 계정·다중기기·실기기는 마지막 Gate)
- WP02-11 raw-once 초대 링크를 redacted canonical HTTPS value object로 제한하고 Android `ACTION_SEND` text chooser를 native host/path/token 재검증과 함께 추가했으며, chooser-open만 성공으로 표현하고 실패 뒤 별도 명시적 write-only clipboard 복구·retention 고지·single-flight·EN/KO/EN-XA 200% UI를 구현했다(DB/API/storage/permission/새 SDK 없음; 실제 share sheet·recipient·verified App Link·다중기기·실기기는 마지막 Gate)
- WP02-12 normalized email과 exact 6자리 OTP의 10분 process-memory challenge, 60초 재전송 제한, request/verify single-flight, strict Supabase session 인계와 generic anti-enumeration 로그인 UI를 EN/KO/EN-XA로 구현하고 local Mailpit token-only bilingual email·synthetic session/reuse/rate-limit을 검증했다(DB migration/API/storage/새 SDK 없음; hosted SMTP·identity policy·실제 mailbox/account·다중기기·실기기는 마지막 Gate)
- WP02-13 authenticated app shell root에 foreground session single-flight와 최대 1회 trailing 재검증을 추가하고, 동일 사용자·동일 household에서는 transient auth event 없이 보호 context를 유지하며 authoritative household 변경·탈퇴·계정 변경·만료·회수·provider failure에서는 local purge/replace와 route fail-close를 수행하도록 구현했다(DB/API/storage/dependency/permission/telemetry 변경 없음; hosted Supabase·실계정·다중 tab/device·Android background·Web BFCache·실기기는 마지막 Gate)
- WP07-07A 설정의 terms/privacy/support 허브, enum-only fixed HTTPS launcher, document-local publication/version authority, informational-only consent 경계, export/account-deletion 바로가기와 EN/KO/EN-XA 200% UI를 구현(최종 법률 copy/version·동의 필요성·owned public site·실browser/screen-reader/실기기는 후속 Gate)
- WP07-08 설정의 로컬 PII-safe 진단 보고서에 exact app/build/environment/coarse-platform/incident allowlist, runtime/config metadata fail-closed 대조, random UUID v4 correlation, write-only explicit clipboard와 EN/KO/EN-XA 200% UI를 구현(signed artifact·실clipboard·screen-reader·remote Sentry/support correlation은 마지막 Gate)
- WP02-08 본인 current adult membership만의 최소 가구 목록과 server-derived member·optimistic version 기반 active household 전환, same-target no-op·private content-free audit·direct update 차단, Settings 확인 UI와 전환 전 read cache/guided resume/pending invite 정리를 구현(local 실패는 content lock; hosted·실계정·다중기기·실기기는 마지막 Gate)
- WP03-08 집안일 생성에 PII-free exact 6-entry 앱 내 빠른 시작 catalog와 ARB localized title·editable daily/weekly repeat를 추가하고 template metadata를 기존 DB/API/cache/analytics 경계 밖에 유지했으며 KO/EN-XA 200%·48dp 회귀를 통과(server/household catalog·onboarding guided setup·실계정/실기기는 후속 Gate)
- WP03-09 scheduled one-time chore의 title·notes·담당자·due date/time 수정과 soft-delete/cancel을 immutable revision, series+occurrence dual expected-version, caller-scoped idempotency, content-free forced-RLS audit, strict Flutter DTO·authoritative reload 및 EN/KO/EN-XA 200% UI로 구현(approval·trash/undo·실계정/remote/두 기기/실기기는 후속 Gate)
- WP03-10 첫 가구 생성 직후 PII-free catalog에서 정확히 세 개의 집안일을 고르고 title·daily/weekly 반복을 수정해 server household date와 current adult 기준으로 기존 recurring RPC를 순차 실행하는 guided setup을 추가했으며, 항목별 unique idempotency·부분 성공 progress/same-key retry·skip 확인·EN/KO/EN-XA 200%를 자동 검증(process-death resume·activation analytics·server catalog·실계정/remote/두 기기/실기기는 후속 Gate)
- WP03-11 distinct adult 참여 2명·chore series 3개·distinct adult completer 2명·household-local day-two를 historical capped aggregate로 파생하는 authenticated RPC와 비차단 Today 진행 카드를 추가하고, identifier/visit 저장 없음·strict DTO·cache 제외·projection retry·완료 후 refresh·cached Today read-only·EN/KO/EN-XA 200%를 자동 검증(정확한 과거 visit analytics·실계정/remote/두 기기/실기기는 마지막 Gate)
- WP03-12 제출된 guided exact-three batch를 전용 backup-disabled secure storage에 strict 8 KiB schema로 저장하고 mutation 전·성공별 checkpoint, controller recreation same-payload/same-key 자동 재개, Today preflight, scope/corrupt purge, logout/account switch cleanup과 clear-before-exit EN/KO/EN-XA UI를 구현(실제 Keystore/Keychain process kill·실계정/remote/다중기기/실기기는 마지막 Gate)
- WP03-13 scheduled one-time 삭제의 process-memory dual-version receipt와 즉시 Undo, exact 18-key bounded `/chores/trash` 목록, deleted/cancelled/once·원래 active assignee·dual expected-version restore, 기존 key namespace replay/cross-operation 충돌·content-free audit·runtime chores guard 및 EN/KO/EN-XA compact 200% UI를 구현(영구 삭제·retention·bulk·repeating/completed restore·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP03-14 반복 집안일 생성·미래 시리즈 편집에 interval `1..30`과 `never|count 1..1000|until` 종료 UI, 생성 anchor·동일-frequency anchor 보존·changed-frequency household-date re-anchor, full-rule retry fingerprint, template reset·until 보정 및 EN/KO/EN-XA compact 200% live summary를 구현(multi-weekday/ordinal/yearly·guided advanced·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP03-15 weekly 반복 집안일 생성·미래 시리즈 편집에 ISO 순서 복수 요일 toggle을 추가하고 생성 start-date anchor 잠금·날짜 변경 anchor 추가·template reset, 시리즈 최소 1개·active set prefill, frequency 왕복 선택 보존·weekday fingerprint 및 EN/KO/EN-XA compact 200%를 구현(multiple month-day·ordinal/yearly·guided advanced·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP03-16 monthly 반복 집안일의 strict 1~31일 selector를 추가하고 생성 due-date anchor 잠금·날짜 변경 연동, 미래 시리즈 active monthDay prefill·변경, changed-to-monthly household-date 초기화·frequency 왕복 보존, skip-not-clamp 정책 안내·monthDay fingerprint 및 EN/KO/EN-XA compact 200%를 구현(multiple month dates·last-day·ordinal/yearly·guided advanced·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP03-17 첫 가구 exact-three guided 설정의 각 항목에 daily/weekly/monthly, interval `1..30`, `never|count 1..1000|until`, frozen-start 복수 weekday와 frozen month-day 편집을 추가하고 full strict rule을 fingerprint·retry payload·secure submitted batch에 보존했으며 secure resume v2 exact JSON과 legacy v1 no-replay cleanup, EN/KO/EN-XA compact 200%를 구현(DB/API/RLS/SDK/permission 변경 없음; remote·실계정·다중기기·실기기는 마지막 Gate)
- WP03-18 active adult용 최근 완료 household-local ISO 주차 12개의 content-free due/completed-by-end/completed-later/open/skipped aggregate와 최대 20명 active contributor·count-only other bucket을 서버 RPC로 추가하고, persistent cache/analytics 없이 Today 비차단 요약·상세 주차 탐색·authoritative 완료 후 refresh·EN/KO/EN-XA compact 200%를 구현(hosted 규모·실계정·두 기기·timezone boundary·실기기는 마지막 Gate)
- WP03-19 기존 stable key를 보존한 PII-free exact 16-entry·5-category 내부 chore template catalog와 localized title 검색·category 교집합 browser를 one-time/repeating 생성 및 first-household exact-three guided setup에 공유하고, hidden selection/draft 보존·KO empty/clear·EN-XA compact 200%·secure resume를 자동 검증했다(DB/API/storage/network/analytics/SDK/permission 변경 없음; remote catalog·추천·개인화·사용자 연구·실계정·실기기는 마지막 Gate)
- WP03-20 Upcoming의 Owner/Admin 반복 회차에 `이 회차부터 수정`을 추가하고 target occurrence의 immutable recurrence slot을 서버 경계로 derivation해 이전·완료 이력을 보존하면서 이후 미완료 회차와 예외를 새 immutable revision 기본값으로 재구성했다. additive RPC의 row lock·expected-version·same-key replay·legacy hash 호환·content-free audit와 strict Flutter DTO/cache/runtime guard/authoritative reload, EN/KO/EN-XA compact 200%를 자동 검증했다(Calendar·선택 회차 이후 취소·hosted·실계정·두 기기·실기기는 마지막/별도 Gate)
- WP03-21 Upcoming의 Owner/Admin 반복 회차에 `이 회차부터 취소`를 추가하고 target의 immutable recurrence slot 이후 미완료 회차만 취소했다. 이전 scheduled prefix가 남으면 source content·anchor를 보존한 bounded terminal revision을 활성화하고 없을 때만 즉시 soft-delete하며, exact version/replay/collision/role/audit/worker no-regeneration과 strict Flutter DTO·cache·runtime guard·authoritative reload, EN/KO/EN-XA compact 200%를 자동 검증했다(immediate process-memory Undo는 WP03-22 후속, persistent history·Calendar 동일 범위·hosted·실계정·두 기기·실기기는 마지막/별도 Gate)
- WP03-22 `이 회차부터 취소` 성공 Snackbar에 process-memory Undo를 추가했다. 기존 cancellation 5-input/9-output 계약을 유지하며 private metadata-only pre-state ledger, original actor의 current Owner/Admin 재인가, exact cancellation-result version/terminal-shape 검증, scheduled/skipped 복원과 새 immutable resumed revision, completed/edited prefix 보존, same-key replay·transient retry·terminal receipt 폐기, strict Flutter DTO/cache/runtime guard와 EN/KO/EN-XA 48dp UI를 자동 검증했다(Calendar immediate parity는 WP04-16에서 구현; process-death history·arbitrary historical resume·hosted·실계정·두 기기·실기기는 마지막/별도 Gate)
- WP04-07 Calendar 생성·one-time 수정·반복 한 회차 수정·전체 시리즈 수정에 same-member timed/all-day/mixed half-open overlap 힌트를 추가하고 제목·설명 없는 bounded RPC, explicit self-exclusion, deterministic 10건 상세와 저장 비차단 checking/none/conflict/unavailable UX를 구현(hosted 규모 query plan·실계정·두 기기·실기기는 마지막 Gate)
- WP04-08 Today를 overdue chores → now/next events → due-today scheduled chores → remaining events → due-today completed chores의 결정적 5구간으로 세분하고 server `generatedAt` 기반 무중복 Calendar partition, 독립 overdue partial failure/retry·quick-complete와 완료 기본 접힘을 구현(production-size latency·remote·실계정·두 기기·실기기는 마지막 Gate)
- WP04-09 기존 Android encrypted read-cache에 exact `today_calendar_v1` fixed slot을 추가하고 Today Calendar cold-process transient fallback, cached-at/read-only/retry, empty-state disclosure, strict domain decode, 모든 Calendar mutation invalidation과 auth/household purge를 구현(Web/iOS persistent cache·offline mutation·실계정·실기기는 마지막 Gate)
- WP04-10 Calendar 생성·전체 시리즈 편집에 interval `1..30`과 `never|count 1..1000|until` 종료 UI, 생성 anchor·동일-frequency multi-weekday/month-day 보존·changed-frequency active-start re-anchor, invalid preview/command 차단, full-rule overlap preview·retry fingerprint 및 EN/KO/EN-XA compact 200% live summary를 구현(multi-weekday 선택·ordinal/yearly·이번 이후 분할·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP04-11 weekly Calendar 생성·전체 시리즈 편집에 ISO 순서 복수 요일 toggle을 추가하고 start-date anchor 잠금, 날짜 변경 anchor 추가, frequency 왕복 선택 보존, active rule prefill, exact overlap preview·weekday fingerprint, localized card/live summary와 EN-XA compact 200%를 구현(multiple month-day·ordinal/yearly·이번 이후 분할·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP04-12 monthly Calendar 생성·전체 시리즈 편집에서 `monthDay`를 event local start date에 항상 재고정하고, 동일-frequency 날짜 변경의 stale anchor를 수정했으며 locked 기준일·skip-not-clamp 안내, exact overlap preview·date/monthDay fingerprint, localized card/live summary와 EN-XA compact 200%를 구현(독립·복수 month date·last-day·ordinal/yearly·이번 이후 분할·실계정/hosted/다중기기/실기기는 마지막 Gate)
- WP04-13 Android SAF에서 사용자가 고른 UTF-8 `.ics`를 256 KiB·50 VEVENT로 제한해 RFC 5545의 all-day·UTC/floating/TZID·DST·daily/weekly/monthly subset을 strict parse하고, 일정/participant 검토·순차 existing create·첫 실패 same-key retry·runtime guard·EN/KO/EN-XA 200% UI를 구현했다(원본/URI/UID persistence·broad permission·provider 계정·sync 없음; 실 document provider·hosted·실계정·두 기기·실기기는 마지막 Gate)
- WP04-14 Calendar scheduled recurring non-exception 회차에 `이 회차부터 수정`을 추가하고 target occurrence의 immutable recurrence slot을 서버 경계로 사용해 이후 source slot만 새 immutable revision으로 재구성했다. 이전 occurrence와 경계 이후 explicit one-occurrence exception을 그대로 보존하고, additive RPC의 target lock·expected-version·same-key replay·legacy signature/hash compatibility와 strict Flutter DTO·Today cache invalidation·runtime guard·boundary editor/overlap preview·authoritative reload·EN/KO/EN-XA를 자동 검증했다(selected Calendar 취소 parity는 WP04-15에서 구현; hosted·실계정·두 기기·timezone boundary·실기기는 마지막 Gate)
- WP04-15 Calendar scheduled recurring non-exception 회차에 `이 회차부터 취소`를 추가하고 target occurrence의 immutable recurrence slot부터 이후 모든 occurrence를 moved explicit exception까지 취소했다. 이전 occurrence·exception은 표시 날짜와 무관하게 보존하고 actionable prefix가 남으면 source snapshot·participant·anchor를 복제한 `until = boundary - 1` terminal revision으로 worker 재생성을 막으며, 없을 때만 시리즈를 경계에서 종료한다. additive RPC의 target lock·expected-version·same-key replay·legacy signature/hash/9-key result compatibility와 strict Flutter DTO·Today cache invalidation·runtime guard·authoritative reload·EN/KO/EN-XA compact 200%를 자동 검증했다(immediate Undo는 WP04-16에서 구현; persistent history·hosted·실계정·두 기기·timezone boundary·DST·실기기는 마지막/별도 Gate)
- WP04-16 selected-boundary Calendar cancellation 성공 Snackbar에 process-memory Undo를 추가했다. 기존 cancellation 5-input/11-output 계약을 유지하며 private metadata-only pre-state ledger, original actor의 current active membership, exact cancellation-result version·terminal/ended shape·suffix post-state 검증, scheduled/completed/skipped와 moved exception semantics 복원, source participant snapshot의 새 immutable resumed revision, later-edited prefix 보존, same-key response-loss retry·terminal receipt 폐기 및 strict Flutter DTO/cache/runtime guard·scrollable EN/KO/EN-XA 48dp UI를 자동 검증했다(persistent history·arbitrary historical resume·hosted·실계정·두 기기·timezone/DST·process-death·실기기는 마지막/별도 Gate)
- WP05-07 timed Calendar 시작 instant와 all-day household-local 09:00 reminder, immutable occurrence revision 참여자 snapshot, 32일 bounded source sweep, exact-audience reschedule/cancel/latest-state inbox·Android push, strict Flutter category pair와 EN/KO/EN-XA 설정 UI를 구현(hosted scheduler·실제 Firebase·실계정·다중기기·실기기는 마지막 Gate)
- WP05-08 Chore inbox와 authorized Android push의 strict subject UUID를 active household로 재인가하는 단건 RPC, no-cache `/chores/occurrence/:occurrenceId` 상세·기존 활동 내역, unavailable/retry recovery와 Calendar exact occurrence routing을 구현했다(missing·forbidden·deleted·skipped는 동일 처리; 실제 Firebase·실계정 membership/delete race·foreground/background/terminated 실기기는 마지막 Gate)
- WP05-09 exact Chore target에 N-1-safe additive action RPC, server-derived 완료 권한, 기존 versioned/idempotent 완료·다시 열기, duplicate/response-loss key 재사용, stale·post-commit authoritative reconciliation과 runtime-policy/a11y UI를 추가했다(실계정 role/assignment·두 기기 race·physical-device notification journey는 마지막 Gate)
- WP05-11 사용자·가구별 Calendar 정시·5·10·15·30·60분 전 설정, timed/all-day base-minus-lead 이후 quiet-hours 순서, 미평가 resolution/pending push 원자적 재스케줄, evaluated history 동결, preference v1 12-key N-1 호환과 strict v2 Flutter EN/KO/EN-XA selector를 구현했다(hosted scheduler·실제 Firebase·실계정·두 기기·실기기는 마지막 Gate; 복수 reminder는 후속이고 bounded Snooze는 WP05-12에서 구현)
- WP05-12 active 본인 Calendar inbox 알림에 5·10·30분 Snooze를 추가하고 연속 최대 3회·occurrence 시작 후 1시간 bound, optimistic item version·UUID command exact replay, 원본 inbox/pending push와 content-free replacement source의 원자적 교체, v1 inbox 호환·strict v2 metadata 및 EN/KO/EN-XA 200% UI를 구현했다(hosted scheduler·실제 Firebase·실계정·두 기기·실기기 timing은 마지막 Gate; arbitrary duration·persistent history·복수 reminder는 후속)
- WP05-13 개인·가구별 Calendar 기본 알림 1개와 distinct fixed 추가 알림 최대 2개, v1 전체 보존·v2 기본-only 호환·strict 14-key v3, content-free source별 private lead identity·독립 worker/quiet-hours/inbox/Snooze/Android push, future-only 추가·pending 재계산·removed stale/evaluated freeze 및 Flutter checkbox editor를 구현했다(hosted scheduler·실제 Firebase·실계정·두 기기·실기기 timing은 마지막 Gate)
- WP05-14 기본 OFF category email preference를 confirmed Auth address의 전송 직전 수명, address-free durable queue, latest-state·quiet-hours·1시간 usefulness 재검사, marker-before-SendGrid-I/O ambiguity quarantine, fixed generic EN/KO payload와 Flutter quick/editor toggle로 연결했다(hosted sender/domain/reputation/quota·실제 mailbox/account/spam·두 기기·실기기는 마지막 Gate)
- WP08-04A dev/prod Android 최소 build·contract와 emergency 전역 non-privacy mutation switch를 private versioned policy·immutable audit·exact public read·direct/Edge-forwarded DB trigger로 구현하고, 앱 initial/retry/resume refresh·읽기/privacy 보존·EN/KO/EN-XA update/read-only banner·provider/network/store I/O 전 advisory guard를 추가(hosted 전파·N-1 signed binary·Play staged rollout·실계정·다중기기·실기기는 마지막 Gate)
- WP08-04B household/chores/calendar/notifications/profile/billing exact mutation switch를 compatibility-open seed·service-only expected-version/correlation audit·30개 explicit DB trigger·`KFR06` Edge mapping으로 구현하고, strict Flutter six-row snapshot·모든 mutation family guard·다른 기능과 읽기를 유지하는 EN/KO/EN-XA partial-read-only banner를 추가(cohort/percentage/per-account targeting·hosted operator drill·실계정·다중기기·실기기는 마지막 Gate)

## v1.2 — 2026-07-22 — Version terminology and SDK patch correction

- `Flutter v1.0`이라는 모호한 표현을 제거하고 `KinFlow 앱 스펙 v1.0`으로 통일
- Flutter SDK 기준선을 `3.44.0`에서 최신 stable hotfix인 `3.44.7`로 갱신
- 포함 Dart SDK를 `3.12.2`로 명시하고 Phase 01에서 `flutter --version` 증거 저장 요구
- `VERSIONING_CONVENTIONS.md` 추가
- 마이그레이션 문서명을 `FROM_EXPO_V0.4_TO_KINFLOW_FLUTTER_SPEC_V1.0.md`로 변경
- 문서팩 버전을 `v1.2`로 갱신

## v1.1 — 2026-07-22 — Markdown-only packaging

- 사람 검토용 DOCX와 전용 review 파일 제거
- 압축 파일 내부 확장자를 `.md`로 통일
- JSON·YAML·SQL·CSV·Dart·TypeScript·Python·환경 변수 예시를 Markdown code fence로 보존
- `MD_ONLY_FORMAT_GUIDE.md`와 원본 경로 매핑 추가
- 계약·검증표 README를 Markdown 래퍼 구조에 맞게 개정
- 제품·기술·Phase의 의미론적 기준선은 KinFlow 앱 스펙 v1.0에서 변경하지 않음

## v1.0 — 2026-07-21

- 클라이언트 기준을 Flutter SDK 3.44.7 stable + Dart SDK 3.12.2로 전환
- 모바일 Store MVP를 iOS/iPadOS/Android로 제한
- Web Companion을 후속 독립 Gate로 이동
- Flutter Desktop 수요 Gate 추가
- Riverpod/go_router/Freezed 기반 계층 구조 확정
- FCM/APNs와 server notification outbox 확정
- GitHub Actions + Fastlane 배포 기준 확정
- 런타임 코드 패치/OTA를 MVP에서 제외
- Astro 공개 사이트 분리
- Flutter 패키지, flavor, code generation, analyzer, 실제 기기 검증 계약 추가
- Expo v0.4 전환 가이드 추가
