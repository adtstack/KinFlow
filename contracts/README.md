# Runtime Contracts

구현에서 직접 읽는 기계 판독 계약을 둔다. Markdown 원본은 `docs/contracts/`에 보존되어 있으며, 현재 WP01-01에서는 승인된 toolchain과 platform/environment 범위만 materialize했다.

- `toolchain.json`: Flutter/Dart, Android, 환경과 identifier 기준

DB/API/domain 계약은 해당 Work Package에서 Markdown 원본과 일치하도록 materialize하고 검증한다.
